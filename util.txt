Cuando agregamos el createdAt en reviews despues de mucho al hace rmigrations
nos pidio 
It is impossible to add the field 'createdAt' with 'auto_now_add=True' to review without providing a default. This is because the database needs something to populate existing rows.
 1) Provide a one-off default now which will be set on all existing rows
 2) Quit and manually define a default value in models.py.

Elegimos opcion 1 y dentro escribimos
timezone.now


DURANTE TODO EL DESAROLLO DEL PROYECTO LA CARPETA DE FRONT ESTUVO AL MISMO NIVEL QUE
ENV
BACKEND

COMO YA TERMINAMOS Y QUEREMOS LA IMPLEMENTACION LO PONEMOS DENTRO DE BACKEND